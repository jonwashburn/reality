#!/usr/bin/env bash
set -euo pipefail

# Ensure Lean toolchain is available (elan)
if [ -f "$HOME/.elan/env" ]; then source "$HOME/.elan/env"; fi

# Fast smoke check (toolchain OK)
lake exe ci >/dev/null

# Run the Prime Closure executable (first run may compile)
lake exe ok


